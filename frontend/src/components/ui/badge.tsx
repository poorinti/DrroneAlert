import { cn } from '../../lib/utils';
export function Badge({ children, tone = 'neutral', className = '' }: { children: React.ReactNode; tone?: string; className?: string }) { const tones: Record<string,string> = { LOW: 'bg-sky-50 text-sky-700', MEDIUM: 'bg-amber-50 text-amber-700', HIGH: 'bg-orange-50 text-orange-700', CRITICAL: 'bg-red-50 text-red-700', neutral: 'bg-slate-100 text-slate-600', blue: 'bg-blue-50 text-blue-700' }; return <span className={cn('inline-flex items-center rounded-full px-2 py-1 text-[10px] font-bold', tones[tone] || tones.neutral, className)}>{children}</span>; }
