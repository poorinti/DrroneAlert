import { Image, Upload } from 'lucide-react';
import { useEffect, useState } from 'react';
import type { Settings } from '../../types';
import { Button } from '../ui/button';
import { Dialog, DialogContent, DialogDescription, DialogTitle } from '../ui/dialog';
import { Input } from '../ui/input';

export function SettingsDialog({ open, settings, onOpenChange, onSave }: { open: boolean; settings: Settings; onOpenChange: (open: boolean) => void; onSave: (form: FormData) => Promise<void> }) { const [title,setTitle] = useState(settings.app_title); const [file,setFile] = useState<File | null>(null); const [preview,setPreview] = useState(''); const [error,setError] = useState(''); const [saving,setSaving] = useState(false); useEffect(() => { setTitle(settings.app_title || 'D DRONE'); setFile(null); setPreview(settings.app_logo_path ? `/uploads/${settings.app_logo_path}` : ''); setError(''); }, [settings, open]); function choose(next: File | null) { setFile(next); if (next) setPreview(URL.createObjectURL(next)); } async function submit(e: React.FormEvent) { e.preventDefault(); if (!title.trim()) return setError('กรุณาระบุชื่อโครงการ'); const form = new FormData(); form.set('appTitle', title.trim()); if (file) form.set('logo', file); setSaving(true); setError(''); try { await onSave(form); onOpenChange(false); } catch (e) { setError(e instanceof Error ? e.message : 'บันทึกไม่สำเร็จ'); } finally { setSaving(false); } } return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent><DialogTitle>ตั้งค่าชื่อและโลโก้</DialogTitle><DialogDescription>ข้อมูลจะแสดงบนแถบนำทางและบันทึกไว้ในระบบ</DialogDescription><form onSubmit={submit} className="mt-5"><label className="text-[11px] font-semibold">ชื่อโครงการ<Input className="mt-1.5" value={title} maxLength={100} onChange={(e) => setTitle(e.target.value)} /></label><label className="mt-4 block text-[11px] font-semibold">โลโก้โครงการ<span className="ml-1 font-normal text-muted">PNG, JPG, WEBP หรือ GIF ไม่เกิน 2 MB</span><span className="mt-1.5 flex cursor-pointer items-center gap-3 rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-3"><span className="grid h-14 w-14 place-items-center overflow-hidden rounded-2xl bg-blue-500 text-white">{preview ? <img className="h-full w-full object-cover" src={preview} alt="ตัวอย่างโลโก้" /> : <Image size={20} />}</span><span className="text-xs font-medium text-slate-600"><Upload className="mb-1" size={15} />เลือกไฟล์โลโก้</span><input type="file" accept="image/png,image/jpeg,image/webp,image/gif" className="sr-only" onChange={(e) => choose(e.target.files?.[0] || null)} /></span></label>{error && <p className="mt-3 rounded-xl bg-red-50 p-2.5 text-[10px] text-red-600">{error}</p>}<Button disabled={saving} className="mt-4 w-full" type="submit">{saving ? 'กำลังบันทึก...' : 'บันทึกการตั้งค่า'}</Button></form></DialogContent></Dialog>; }
